import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // experimental: {
  //   serverActions:{
  //     o
  //   }
  // },
  // webpack: (config) => {
  //   config.resolve.fallback = {
  //     ...config.resolve.fallback,
  //     buffer: require.resolve("buffer/"),
  //   };
  //   config.externals = {
  //     "node-appwrite": "commonjs node-appwrite",
  //   };
  //   return config;
  // },
};

export default nextConfig;
